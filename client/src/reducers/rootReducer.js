
const initialStates = {
    user: null,
    currentChat: null
  };
  

function reducer (state = initialStates, action) {   // Pure function!
    console.log(action);
    // switch(action.type) {
     
    // }
    return state;
  }
  

  export default reducer