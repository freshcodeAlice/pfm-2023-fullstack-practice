const initialStates = false;

function fetchingReducer(state = initialStates, action) {
    switch (action.type) {
        default: {
            return state;
        }
    }
}

export default fetchingReducer;